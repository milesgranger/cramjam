[target.x86_64-unknown-linux-musl]
pre-build = [
  "apt update",
  "apt install -y nasm"
]
[target.s390x-unknown-linux-gnu]
pre-build = [
  "apt update",
  "apt install -y nasm"
]
[target.aarch64-unknown-linux-musl]
pre-build = [
  "apt update",
  "apt install -y nasm"
]
