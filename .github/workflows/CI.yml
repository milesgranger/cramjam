name: CI

on:
  pull_request:
  release:
    types:
      - released
      - prereleased

jobs:
  build-test:
    runs-on: ${{ matrix.os }}
    name: ${{ matrix.os }}-${{ matrix.target }}-${{ matrix.package.name }}-${{ matrix.python-version }}-${{ matrix.python-architecture }}-${{ matrix.manylinux }}-${{ matrix.features }}
    strategy:
      fail-fast: false
      matrix:
        python-version: 
          - '3.8' 
          # - '3.9' 
          # - '3.10'
          # - '3.11'
          # - '3.12'
          # - 'pypy3.9'
          # - 'pypy3.10'
        package:
          - dir: cramjam-python
            name: cramjam
          - dir: cramjam-cli
            name: cramjam-cli
        os:
          - ubuntu-latest
          - macos-13
          - windows-latest
        target:
          - x86_64
          - aarch64
          - universal2
          - i686
          - armv7
          - s390x
          - ppc64le
        manylinux:
          - auto
          - musllinux_1_1
        python-architecture:
          - x86
          - x64
        features:
          - '--features experimental'

        exclude:
          # TODO: to get blosc2 out of experimental, it probably needs to compile on these
          #       but fails w/, for example, crc feature modifier on aarch64
          #       official blosc2 dist has aarch64 so maybe re-use that?
          - { target: aarch64, features: '--features experimental'}
          - { target: armv7, features: '--features experimental'}
          - { target: s390x, features: '--features experimental'}
          - { target: ppc64le, features: '--features experimental'}

          # Unsupported PyPy
          - { target: i686, python-version: 'pypy3.9' }
          - { target: i686, python-version: 'pypy3.10' }
          - { target: armv7, python-version: 'pypy3.9' }
          - { target: armv7, python-version: 'pypy3.10' }
          - { target: s390x, python-version: 'pypy3.9' }
          - { target: s390x, python-version: 'pypy3.10' }
          - { target: ppc64le, python-version: 'pypy3.9' }
          - { target: ppc64le, python-version: 'pypy3.10' }

          # Unsupported linux configs
          - { os: ubuntu-latest, python-architecture: x86 }
          - { os: ubuntu-latest, target: universal2 }
          - { os: ubuntu-latest, target: s390x, manylinux: musllinux_1_1 }    # No target s390x-unknown-linux-musl
          - { os: ubuntu-latest, target: ppc64le, manylinux: musllinux_1_1 }  # No target powerpc64le-unknown-linux-musl

          # Unsupported windows configs
          - { os: windows-latest, manylinux: musllinux_1_1 }
          - { os: windows-latest, python-architecture: x86, target: x86_64 }
          - { os: windows-latest, python-architecture: x64, target: i686 }
          - { os: windows-latest, target: universal2 }
          - { os: windows-latest, target: armv7 }
          - { os: windows-latest, target: s390x }
          - { os: windows-latest, target: ppc64le }
          - { os: windows-latest, target: aarch64 }  # TODO: this should work

          # Unsupported OSX configs
          - { os: macos-13, manylinux: musllinux_1_1 }
          - { os: macos-13, python-architecture: x86 }
          - { os: macos-13, target: armv7 }
          - { os: macos-13, target: s390x }
          - { os: macos-13, target: i686 }
          - { os: macos-13, target: ppc64le }
          
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          architecture: ${{ matrix.python-architecture }}

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: ${{ matrix.os }}-${{ matrix.target }}-${{ matrix.python-architecture }}-${{ matrix.features }}

      - name: Setup (Mac)
        if: runner.os == 'macOS'
        run: |
          brew install ninja

          rustup target add aarch64-apple-darwin
          rustup target add x86_64-apple-darwin

          echo "MACOSX_DEPLOYMENT_TARGET=10.12" >> $GITHUB_ENV
          echo "DYLD_FALLBACK_LIBRARY_PATH=$(pwd)/blosc2/lib64:$(pwd)/blosc2/lib" >> $GITHUB_ENV

          # blosc2-rs has a difficult time compiling on x86_64 mac w/ missing symbol __cpu_model.
          # and python less than 3.10 is not available on macos-14 M1. So we'll grab the already compiled one
          pip download --platform macosx_10_9_x86_64 --only-binary=:all: blosc2==2.6.2 --dest out --no-deps --python-version 3.12
          unzip out/blosc2-2.6.2-cp312-cp312-macosx_10_9_x86_64.whl
          echo "BLOSC2_INSTALL_PREFIX=$(pwd)/blosc2-2.6.2.data/data" >> $GITHUB_ENV

      - name: Setup (Windows)
        if: runner.os == 'Windows'
        run: |
          choco install ninja cmake
          rustup target add i686-pc-windows-msvc

          echo "BLOSC2_INSTALL_PREFIX=${{ github.workspace }}\build" | Out-File -FilePath $Env:GITHUB_ENV -Encoding utf8 -Append

          $Env:PATH += ";${{ github.workspace }}\build\lib;${{ github.workspace }}\build\lib64;${{ github.workspace }}\build\bin"
          echo "PATH=$Env:PATH" | Out-File -FilePath $Env:GITHUB_ENV -Encoding utf8 -Append

          $Env:LIBPATH += ";${{ github.workspace }}\build\lib;${{ github.workspace }}\build\lib64;${{ github.workspace }}\build\bin"
          echo "LIBPATH=$Env:LIBPATH" | Out-File -FilePath $Env:GITHUB_ENV -Encoding utf8 -Append

          $Env:LIB += ";${{ github.workspace }}\build\lib;${{ github.workspace }}\build\lib64;${{ github.workspace }}\build\bin"
          echo "LIB=$Env:LIB" | Out-File -FilePath $Env:GITHUB_ENV -Encoding utf8 -Append

      - name: Setup (Ubuntu)
        if: runner.os == 'Linux'
        run: |
          sudo apt update
          sudo apt install ninja-build -y

      - name: Set up Clang (Ubuntu)
        if: runner.os == 'Linux'
        uses: egor-tensin/setup-clang@v1
        with:
          version: 17

      - name: Audit
        if: runner.os == 'Linux'
        run: cargo install cargo-audit && cargo audit

      - name: Rust Tests (Linux & Windows)
        if: matrix.os != 'macos-13' && matrix.target == 'x86_64'
        run: cargo test --no-default-features --release ${{ matrix.features }}

      - name: Rust Tests (MacOS)
        if: matrix.os == 'macos-13' && matrix.target == 'x86_64'
        run: cargo test --no-default-features --release ${{ matrix.features }} --features use-system-blosc2

      - name: Build wheel (Linux)
        if: runner.os == 'Linux'
        uses: PyO3/maturin-action@v1
        with:
          target: ${{ matrix.target }}
          manylinux: ${{ matrix.manylinux }}
          args: -i ${{ matrix.python-version }} --release --out dist --manifest-path ${{ matrix.package.dir }}/Cargo.toml ${{ matrix.features }}
          # before-script-linux: apt install clang -y || yum install clang -y

      - name: Build wheel (Windows)
        if: runner.os == 'Windows'
        run: |
          python -m pip install maturin delvewheel

          maturin build `
            -i python `
            --release `
            --out dist `
            --target ${{ matrix.target }}-pc-windows-msvc `
            --manifest-path ${{ matrix.package.dir }}/Cargo.toml ${{ matrix.features }}

          # Repair wheel
          $file = Get-ChildItem -Path ".\dist\" -Filter "*.whl" | Select-Object -First 1
          if ($file -ne $null) {
              & delvewheel repair -v $file.FullName -w dist
          } else {
              Write-Host "No .whl file found in the dist directory."
          }

          # Remove location of blosc2 to ensure fixing wheel works
          Remove-Item -Path $env:BLOSC2_INSTALL_PREFIX -Recurse -Force -ErrorAction SilentlyContinue

      - name: Build wheel (MacOS)
        if: runner.os == 'macOS'
        run: |
          python -m pip install maturin delocate

          maturin build \
            -i python \
            --release \
            --out dist \
            --sdist \
            --target ${{ matrix.target }}-apple-darwin \
            --features use-system-blosc2 \
            --manifest-path ${{ matrix.package.dir }}/Cargo.toml ${{ matrix.features }}

          delocate-wheel -v dist/*.whl

          # Ensure wheel isn't linking to something we've built outside of it
          rm -rf $BLOSC2_INSTALL_PREFIX

      - name: Install built wheel and Test (Native)
        if: |
          !startsWith(matrix.manylinux, 'musl') &&
          (matrix.target == 'x86_64' || matrix.target == 'universal2')
        run: |
          # TODO: Remove after next release of cramjam-cli
          python -m pip install pytest-benchmark cramjam

          # Second install guarantees it's going to install from local dir w/ --no-index
          # use first to get in dev dependencies
          python -m pip install ${{ matrix.package.name }}[dev] --pre --find-links dist --force-reinstall
          python -m pip install ${{ matrix.package.name }} --pre --no-index --find-links dist --force-reinstall


          python -m pytest ${{ matrix.package.dir }} -vs --benchmark-skip

      - name: Install built wheel and Test (Cross)
        if: |
          !startsWith(matrix.manylinux, 'musl') && 
          runner.os == 'Linux' &&
          contains(fromJson('["armv6", "armv7", "aarch64", "riscv64", "s390x", "ppc64le"]'), matrix.target) 
        uses: uraimo/run-on-arch-action@v2.7.1
        with:
          arch: ${{ matrix.target }}
          distro: ubuntu20.04
          githubToken: ${{ github.token }}
          # Mount the dist directory as /artifacts in the container
          dockerRunArgs: |
            --volume "${PWD}/dist:/artifacts"
          install: |
            apt-get update
            apt-get install -y --no-install-recommends python3 python3-venv software-properties-common
            add-apt-repository ppa:deadsnakes/ppa
            apt-get update
            apt-get install -y curl python${{ matrix.python-version }}-venv
          run: |
            ls -lrth /artifacts
            PYTHON=python${{ matrix.python-version }}
            $PYTHON -m venv venv
            venv/bin/pip install -U pip
            venv/bin/pip install ${{ matrix.package.name }} --pre --no-index --find-links /artifacts --force-reinstall

            venv/bin/python -c 'import cramjam' || venv/bin/cramjam-cli --help

      - name: Upload wheels
        uses: actions/upload-artifact@v4
        with:
          name: wheels
          path: dist
          overwrite: true

  pypi-publish:
      name: Upload ${{ matrix.package }} release to PyPI
      strategy:
        fail-fast: false
        matrix:
          package: 
            - cramjam
            - cramjam-cli
      runs-on: ubuntu-latest
      if: "startsWith(github.ref, 'refs/tags/')"
      needs: [build-test]
      environment:
        name: pypi
        url: https://pypi.org/p/${{ matrix.package }}
      permissions:
        id-token: write
      steps:
      - uses: actions/download-artifact@v4
        with:
          name: wheels
      - name: Separate packages into directories
        run: |
          ls -l

          mkdir artifacts-cramjam-cli
          mv cramjam_cli* artifacts-cramjam-cli/
          ls -l artifacts-cramjam-cli/

          mkdir artifacts-cramjam
          mv cramjam* artifacts-cramjam/
          ls -l artifacts-cramjam/

          ls -l

      - name: Publish package distributions to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          skip-existing: true
          packages-dir: artifacts-${{ matrix.package }}/
